import "./styles.css";

/**
 * 問題　四則演算を行うメソッドを持ったオブジェクトを
 * クロージャーを用いて作成
 *
 * 四則演算を行うメソッド(plus,minus,multiply,divied)を
 * 実行すると以下のようにコンソールに表示される
 *
 * const calc = calcFactory(10) //初期値を10として設定
 * calc.plus(5) //出力結果 "10 + 5 = 15"
 * calc.minus(3) //出力結果 "15 - 3 = 12"
 * calc.multiply(3) //出力結果 "12 * 3 = 36"
 * calc.divide(2) //出力結果 "36 / 2 = 18"
 *
 * ※前に行った計算結果をもとに四則演算を行います。
 * ※四則演算は+,-,*,/を数値で挟んで計算を行います。
 */

//  function calcFactory(num){

//  }
